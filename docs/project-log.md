# Project Log

按时间倒序记录重要调整、关键决策和阶段里程碑。

## 2026-04-02

- 将原先嵌套在 `literature-manager/` 下的实际项目整体提升到仓库根目录，仓库根目录现在就是唯一项目根目录。
- 将项目对外命名切换为 `Knowledge Vault`，建议未来 GitHub 仓库名使用 `knowledge-vault`。
- 新增 `README.md`、`AGENTS.md` 和新的 `docs/` 文档体系，用于对外展示、内部维护和 Agent 快速恢复上下文。
- 将旧的顶层说明文档拆分为多个职责明确的文档，减少信息堆叠和重复。
- 更新采集运行时路径逻辑与相关测试，使其适配新的扁平化目录结构。
- 新增独立的“视频转笔记”模块，支持后台任务式执行 `yt-dlp -> Whisper -> Gemini` 工作流。
- Gemini 运行时支持从仓库根目录 `.env` 读取 `GEMINI_PROXY_URL`，供翻译、期刊总结和视频笔记统一走本地代理。
- 为视频模块补充前端页面、后端 API、任务模型、日志模型和任务产物目录规则。

## 2026-03-04

- 将采集能力整合进主项目，形成统一的前后端分离结构。
- 明确采集中心采用数据库主存储 + 文件派生产物的模式。
- 引入采集子域分层设计，统一模型、仓储、服务、Provider 与路由边界。
