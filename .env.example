# Optional: use env vars instead of backend/gemini_api_key.txt
GEMINI_API_KEY=

# 模型配置页面保存的 API Key；示例文件必须保持为空映射。
LLM_API_KEYS_JSON={}

# Elsevier Research Products API Key（Scopus 题录采集）
ELSEVIER_API_KEY=

# Optional standard proxy variables. Fill in the local HTTP proxy URL when needed.
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1,::1
